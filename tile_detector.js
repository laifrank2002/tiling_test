// adds an event listener that detects which tile in a grid is being clicked based on set parameters
/*
legacy
var tile_detector = (
	function()
	{
		
		return {
			getMouseClick: function (event)
			{
				var x = event.offsetX;
				var y = event.offsetY;
				engine.log("Mouse clicked at: [" + x + "," + y + "]");
			}
		}
	}
)();
*/